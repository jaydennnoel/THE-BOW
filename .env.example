# The Bow has no backend and nothing to configure.
#
# The site is entirely static: no reservation endpoint, no booking provider,
# no form posts anywhere. If you later add something that needs a key, put it
# here as VITE_… and Vite will expose it to the client bundle.
